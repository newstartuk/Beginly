# Beginly Mobile

Expo/React Native source implementation for the native companion. It consumes the same web API contracts for Today, Journey, Opportunities and Nia.

This environment did not install the Expo dependency tree or produce signed Android/iOS binaries. Run `npm install` in this folder, set `EXPO_PUBLIC_BEGINLY_API_URL`, then execute `npm run android` or `npm run ios`.
