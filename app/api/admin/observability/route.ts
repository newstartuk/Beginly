import { NextRequest,NextResponse } from "next/server";import{requireApiActor,apiFailure}from"@/lib/platform/api-auth";import{requireAdminRole}from"@/lib/platform/admin-operations";import{readPlatformEvents,platformObservabilityHealth}from"@/lib/platform/observability";
export async function GET(request:NextRequest){let requestId:string|undefined;try{const actor=await requireApiActor(request);requestId=actor.requestId;await requireAdminRole(actor,["support_admin"]);const limit=Math.min(500,Math.max(1,Number(request.nextUrl.searchParams.get("limit")??100)));const[events,health]=await Promise.all([readPlatformEvents(limit),platformObservabilityHealth()]);return NextResponse.json({health,events,productionVerified:false},{headers:{"x-request-id":requestId}})}catch(error){return apiFailure(error,requestId)}}
