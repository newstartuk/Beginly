import type { Metadata } from "next";import Link from "next/link";import JsonLd from "@/components/seo/JsonLd";import { PUBLIC_ROUTES } from "@/lib/seo/public-content";
export const metadata:Metadata={title:"Adaptive UK journeys | Beginly",description:"Explore Beginly’s adaptive Student, Graduate, Skilled Worker, Health and Care, Family and Founder journeys.",alternates:{canonical:"/routes"}};
export default function RoutesPage(){return <main className="public-page"><JsonLd data={{"@context":"https://schema.org","@type":"CollectionPage",name:"Beginly adaptive journeys",url:"https://beginly.app/routes",hasPart:PUBLIC_ROUTES.map(item=>({"@type":"WebPage",name:item.title,url:`https://beginly.app/routes/${item.slug}`}))}}/><section className="public-hero"><span className="eyebrow">One identity · evolving journeys</span><h1>Beginly changes with the route and life stage.</h1><p>Start from the context that matters now. Your account, history, household and future preparation continue when that context changes.</p></section><section className="route-option-grid">{PUBLIC_ROUTES.map(item=><Link key={item.slug} href={`/routes/${item.slug}`} className="public-route-card"><strong>{item.name}</strong><span>{item.description}</span></Link>)}</section></main>}
